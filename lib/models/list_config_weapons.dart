class ListConfigWeapons {
  num fireRate;
  num magazineSize;
  num equipSpeed;
  num reloadSpeed;

  ListConfigWeapons(
      {required this.fireRate,
      required this.magazineSize,
      required this.equipSpeed,
      required this.reloadSpeed});
}
