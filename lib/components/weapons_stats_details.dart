import 'package:flutter/material.dart';

import 'card_stats.dart';

class WeaponsStatsDetails extends StatelessWidget {
  const WeaponsStatsDetails({super.key});

  @override
  Widget build(BuildContext context) {
    return CardStatsWidget(
      child: Column(),
    );
  }
}
